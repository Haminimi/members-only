<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Sign Up</title>
		<link rel="stylesheet" href="/stylesheets/style.css" />
		<link
			href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300..700&family=Roboto&family=Satisfy&display=swap"
			rel="stylesheet"
		/>
		<link rel="icon" href="images/message.ico" />
	</head>
	<body>
		<div class="sign-up-container">
			<div class="sign-up-form-container">
				<form action="" method="POST" class="sign-up-form">
					<div class="form-group">
						<label for="username">Username</label>
						<input name="username" placeholder="johndoe" type="text"
						<%= typeof username !== 'undefined' ?
						`value=${username}` : '' %> minlength="1" required/>
					</div>
					<div class="form-group">
						<label for="first">First name</label>
						<input name="first" placeholder="John" type="text" <%=
						typeof first !== 'undefined' ? `value=${first}` : '' %>
						minlength="1" required />
					</div>
					<div class="form-group">
						<label for="last">Last name</label>
						<input name="last" placeholder="Doe" type="text" <%=
						typeof last !== 'undefined' ? `value=${last}` : '' %>
						minlength="1" required />
					</div>
					<div class="form-group">
						<label for="password">Password</label>
						<input
							id="password"
							name="password"
							type="password"
							minlength="8"
							required
						/>
					</div>
					<div class="form-group">
						<label for="confirm">Confirm password</label>
						<input
							id="confirm"
							name="passwordConfirmation"
							type="password"
							required
						/>
					</div>
					<button type="submit" class="sign-up-button">
						Sign Up
					</button>
				</form>
				<%if (typeof errors !== 'undefined') {%>
				<div class="sign-up-errors">
					<% errors.forEach(error => { %>
					<p><%= error.msg %></p>
					<% }) %>
				</div>
				<% } %>
			</div>
		</div>
	</body>
</html>
